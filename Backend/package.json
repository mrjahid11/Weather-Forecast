{
  "name": "weather-backend",
  "version": "1.0.0",
  "description": "Express backend for weather forecast (geocoding via Nominatim, weather via Open-Meteo)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "express-rate-limit": "^6.8.0",
    "node-cache": "^5.1.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
